# Git Remove Path History
A tool for removing git changes made to some path in your repo. You can specify a reference in the git
history for it to remove all the changes to that path back until that point.

# Usage
Make the script executable: `chmod +x git-remove-path-history.sh`.
Then run the script without arguments to see the help and instructions: `./git-remove-path-history.sh`
